import Image from "next/image";
import { DogzeProduct } from "@/lib/products";
export function ProductCard({product,selected,onSelect}:{product:DogzeProduct;selected:boolean;onSelect:()=>void}){return <button type="button" onClick={onSelect} className={`w-full rounded-2xl border p-3 text-left transition active:scale-[0.99] ${selected?"border-dogze-orange bg-dogze-orange/10 dogze-glow":"border-white/10 bg-dogze-panel"}`}><div className="flex gap-4"><div className="relative h-24 w-24 shrink-0 overflow-hidden rounded-xl bg-black/30"><Image src={product.image} alt={product.name} fill className="object-contain p-2"/></div><div className="min-w-0 flex-1"><p className="text-xs font-black uppercase tracking-[0.22em] text-dogze-orange">{product.type}</p><h3 className="mt-1 text-lg font-black leading-6 text-white">{product.name}</h3><p className="mt-2 text-sm leading-5 text-dogze-muted">{product.description}</p><p className="mt-2 text-base font-black text-white">{product.price}</p></div></div></button>}
